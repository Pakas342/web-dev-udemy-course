
function whosPaying(names) {
    
    /******Don't change the code above*******/
        
        //Write your code here.
        
        var payer = (Math.random() * names.length);
        payer = Math.floor(payer);
        return names[payer] + ' is going to buy lunch today!'
        
        
    
    
    /******Don't change the code below*******/    
    }